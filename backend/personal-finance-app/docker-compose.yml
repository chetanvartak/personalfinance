version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./personal_finance.db
    volumes:
      - ./:/app
  # Add a db service if you plan to use Postgres in prod
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_USER: pguser
  #     POSTGRES_PASSWORD: pgpass
  #     POSTGRES_DB: personal_finance
